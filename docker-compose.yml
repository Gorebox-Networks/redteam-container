version: '3.8'

services:
  redteam:
    image: redteam
    container_name: redteam
    hostname: redteam
    build:
      context: ./redteam
      args:
        SSHPASSWORD: ${SSHPASSWORD}
    ports:
      - "${RTSSHHOSTPORT}:22"
    restart: unless-stopped
    env_file:
      - .env
